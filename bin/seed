#!/bin/sh

# bin/seed: Create seed data

set -e

if psql -lqt | cut -d \| -f 1 | grep -w challenges_dev; then
  dropdb challenges_dev
fi

createdb challenges_dev

# Add migrations
./bin/migrate

# Add Seed Data
echo "==> Adding Seed Data..."
node db/seed
echo "==> Done!"
