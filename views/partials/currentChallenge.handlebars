{{#if currentChallenge}}
<hr />
<h2>You're currently signed up to challenge spot {{currentChallenge.SpotId}} for the {{user.nextPerformance.name}}</h2>
{{else}}
  {{#if user.eligible}}
		<hr />
    <h2>You need to make a challenge! Head to Make A Challenge to do that</h2>
  {{/if}}
{{/if}}
