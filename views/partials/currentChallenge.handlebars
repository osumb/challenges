{{#if challenge}}
<h2 class="CurrentChallenge">You're currently signed up to challenge spot {{challenge.spotId}} for the {{challenge.performanceName}}</h2>
{{else}}
  {{#if canChallenge}}
    <h2 class="CurrentChallenge">You need to make a challenge! Head to Make A Challenge to do that</h2>
  {{/if}}
{{/if}}
