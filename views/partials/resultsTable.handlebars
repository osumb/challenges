<table class="table">
  <thead>
      <tr>
        <th>Performance</th>
        <th>Opponent</th>
        <th>Won?</th>
        <th>Comments</th>
      </tr>
  </thead>
  {{#each results}}
    <tr id="row-id-"{{@index}}>
      <td>{{this.PerformanceName}}</td>
      <td>{{this.opponentName}}</td>
      {{#if this.winner}}
        <td>:)</td>
      {{else}}
        <td>:(</td>
      {{/if}}
      <td>{{this.comments}}</td>
    </tr>
  {{/each}}
</table>
